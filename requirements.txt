# Runtime dependencies for the Streamlit Cloud demo.
# pyproject.toml stays the source of truth for local development and CI.
# This file is the subset app.py actually imports: the FastAPI service,
# the PDF reporting stack and the PostgreSQL driver are not needed because
# the demo runs the dashboard alone on SQLite.
streamlit>=1.60.0
pandas>=2.2.0
numpy>=2.5.0
plotly>=6.8.0
sqlalchemy>=2.0.51
pydantic>=2.13.0
